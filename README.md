# Named Entity Recognition (NER) — CoNLL-2003

This project implements a Named Entity Recognition (NER) system based on the English CoNLL-2003 corpus. A bidirectional recurrent neural network (BiLSTM) is used for sequence labeling.

## Source Repository

The task is based on the repository:
https://git.wmi.amu.edu.pl/kubapok/en-ner-conll-2003

The training data is located in `train/train.tsv.xz`.
